from models.position import Position

def rate_position(position_id: str):
    # index = llm.load_index()
    # position = Position.get_one(position_id)
    # return llm.query(index, "Who Is the best candidate for the role of "+  position.name + " - " + position.description)
    pass